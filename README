# Bindings for WinDivert 1.4.3

Includes bundled sources and precompiled signed driver files from WinDivert 1.4.3-B.
Statically links windivert. Documentation located here: https://reqrypt.org/windivert-doc.html.
Examples must be run as an admin in order to load the filter driver, in addition, the driver
SYS files must be placed in the same directory as the executable, placing the driver files
in the current working directory will not work.

This library is a no-op on non-windows platforms.